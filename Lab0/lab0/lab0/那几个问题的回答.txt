	他们都是面向对象的高级语言，在这个例子中，读写文件比C容易很多。语法也比较相似。感触最深的不同就是C++中保留了大量的C的特点，感觉是种高级语言和低级语言的混合产物，而java就只有高级语言的那一部分。还有一个具体的不同就是关于异常处理，java中只要可能出现Exception，就会强制我们try catch，但是C++中不会强制我们添加，这个就比较麻烦了，如果忘了加try catch ，出错都不知道是哪错的。
